# Server-side only. Never rename these with a VITE_ prefix.
OPENAI_API_KEY=
OPENAI_NEWS_MODEL=gpt-5.4-mini

# Used only when the OpenAI edition fails validation.
GROQ_API_KEY=
GROQ_NEWS_MODEL=groq/compound
